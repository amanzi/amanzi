name: Amanzi CI

on: [push]

jobs:
  build_test:
    runs-on: ubuntu-latest
    name: Build and test with Docker
    steps:
    - name: Check out the Amanzi repo
      uses: actions/checkout@v2
    - name: Extract the branch name
      id: branch
      working-directory: Docker
      run: echo "::set-output name=AMANZI_BRANCH::$(echo $GITHUB_REF | sed -e 's/refs\/heads\///')"
    - name: Output the branch name
      run:
        echo "Branch or Tag reference = ${{steps.branch.outputs.AMANZI_BRANCH}}"
    - name: Filter the branch name
      id: filter
      uses: frabert/replace-string-action@v1.2
      with:
        pattern: '\/'
        string: "${{steps.branch.outputs.AMANZI_BRANCH}}"
        replace-with: '-'
        replaced: amanzi_branch_tag
    - name: Get TPLs version
      id: version 
      working-directory: Docker
      run: echo "::set-output name=AMANZI_TPLS_VER::$(./get_tpls_version.sh)"
    - name: Output the TPLs version
      run: echo "TPLs version = ${{steps.version.outputs.AMANZI_TPLS_VER}}"
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}      
    - name: Docker build
      id: docker
      working-directory: Docker
      run: docker build --build-arg amanzi_branch=${{steps.branch.outputs.AMANZI_BRANCH}} --build-arg amanzi_tpls_ver=${{steps.version.outputs.AMANZI_TPLS_VER}} -t metsi/amanzi:$amanzi_branch_tag-latest -f Dockerfile-Amanzi-build .
    - name: Docker push
      working-directory: Docker
      run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/amanzi:${{steps.branch.outputs.AMANZI_BRANCH}}-latest


