#  -*- mode: cmake -*-

#
# TPLVersions
#    Define the versions, approved download locations for each TPL
#
#


#
# TPL: CURL
#
set(CURL_VERSION_MAJOR 7)
set(CURL_VERSION_MINOR 21)
set(CURL_VERSION_PATCH 6)
set(CURL_VERSION ${CURL_VERSION_MAJOR}.${CURL_VERSION_MINOR}.${CURL_VERSION_PATCH})
set(CURL_URL_STRING     "http://curl.haxx.se/download")
set(CURL_ARCHIVE_FILE   curl-${CURL_VERSION}.tar.gz)
set(CURL_MD5_SUM       c502b67898b4a1bd687fe1b86419a44b) 

if(TPL_DOWNLOAD_DIR)
  set(CURL_DOWNLOAD_LOCATION ${TPL_DOWNLOAD_DIR} )
else()  
  set(CURL_DOWNLOAD_LOCATION ${CURL_URL_STRING})
endif() 

#
# TPL: zlib
#
set(ZLIB_VERSION_MAJOR 1)
set(ZLIB_VERSION_MINOR 2)
set(ZLIB_VERSION_PATCH 5)
set(ZLIB_VERSION ${ZLIB_VERSION_MAJOR}.${ZLIB_VERSION_MINOR}.${ZLIB_VERSION_PATCH})
set(ZLIB_URL_STRING     "http://zlib.net")
set(ZLIB_ARCHIVE_FILE   zlib-${ZLIB_VERSION}.tar.gz)
set(ZLIB_MD5_SUM        c735eab2d659a96e5a594c9e8541ad63) 

if(TPL_DOWNLOAD_DIR)
  set(ZLIB_DOWNLOAD_LOCATION ${TPL_DOWNLOAD_DIR} )
else()  
  set(ZLIB_DOWNLOAD_LOCATION ${ZLIB_URL_STRING})
endif() 

#
# TPL: METIS
#
set(METIS_VERSION_MAJOR 5)
set(METIS_VERSION_MINOR 0)
set(METIS_VERSION_PATCH 2)
set(METIS_VERSION ${METIS_VERSION_MAJOR}.${METIS_VERSION_MINOR}.${METIS_VERSION_PATCH})
set(METIS_URL_STRING     "http://glaros.dtc.umn.edu/gkhome/fetch/sw/metis")
set(METIS_ARCHIVE_FILE   metis-${METIS_VERSION}.tar.gz)
set(METIS_MD5_SUM        acb521a4e8c2e6dd559a7f9abd0468c5) 

if(TPL_DOWNLOAD_DIR)
  set(METIS_DOWNLOAD_LOCATION ${TPL_DOWNLOAD_DIR} )
else()  
  set(METIS_DOWNLOAD_LOCATION ${METIS_URL_STRING})
endif() 

