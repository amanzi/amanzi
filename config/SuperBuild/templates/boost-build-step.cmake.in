execute_process(
  COMMAND @Boost_source_dir@/bjam --prefix=@Boost_install_dir@ --build-dir=@Boost_build_dir@ install
  WORKING_DIRECTORY "@Boost_source_dir@"
  RESULT_VARIABLE exit_code
)

if ( NOT ${exit_code} EQUAL 0 )
    message(FATAL_ERROR "Boost build step failed")
endif()

