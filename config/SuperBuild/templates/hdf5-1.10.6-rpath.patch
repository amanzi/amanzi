diff -ruNbB hdf5-1.10.6/config/cmake_ext_mod/HDFMacros.cmake hdf5-1.10.6-source/config/cmake_ext_mod/HDFMacros.cmake
--- hdf5-1.10.6/config/cmake_ext_mod/HDFMacros.cmake	2019-12-17 15:14:09.000000000 -0700
+++ hdf5-1.10.6-source/config/cmake_ext_mod/HDFMacros.cmake	2019-12-28 13:38:51.000000000 -0700
@@ -379,15 +379,16 @@
   set (CMAKE_INSTALL_RPATH_USE_LINK_PATH  FALSE)
   # add the automatically determined parts of the RPATH
   # which point to directories outside the build tree to the install RPATH
-  set (CMAKE_BUILD_WITH_INSTALL_RPATH ON)
+  set (CMAKE_BUILD_WITH_INSTALL_RPATH OFF)
   if (APPLE)
-    set (CMAKE_INSTALL_NAME_DIR "@rpath")
-    set (CMAKE_INSTALL_RPATH
-        "@executable_path/../${${package_prefix}_INSTALL_LIB_DIR}"
-        "@executable_path/"
-        "@loader_path/../${${package_prefix}_INSTALL_LIB_DIR}"
-        "@loader_path/"
-    )
+    set (CMAKE_MACOSX_RPATH ON)
+    # set (CMAKE_INSTALL_NAME_DIR "@rpath")
+    # set (CMAKE_INSTALL_RPATH
+    #    "@executable_path/../${${package_prefix}_INSTALL_LIB_DIR}"
+    #    "@executable_path/"
+    #    "@loader_path/../${${package_prefix}_INSTALL_LIB_DIR}"
+    #    "@loader_path/"
+    #)
   else ()
     set (CMAKE_INSTALL_RPATH "\$ORIGIN/../${${package_prefix}_INSTALL_LIB_DIR}:\$ORIGIN/")
   endif ()
