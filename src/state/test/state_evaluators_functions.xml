<ParameterList name="main">
  <ParameterList name="regions">
    <ParameterList name="all">
      <ParameterList name="region: all">
      </ParameterList>
    </ParameterList>
  </ParameterList>
  
  <ParameterList name="state">
    <ParameterList name="evaluators">
      <!-- NOTE: based on 2D mesh with 2 cells on x in (0,4), so centroids at 1 and 3. -->
      <!-- NOTE: based on time values of 0, 1, 2, 3, 4 -->
      <ParameterList name="field1">
        <!-- values(all time) = (1, 3) -->
	<Parameter name="evaluator type" type="string" value="independent variable"/>
	<ParameterList name="function">
          <ParameterList name="domain">
            <Parameter name="region" type="string" value="all"/>
            <Parameter name="component" type="string" value="cell"/>
            <ParameterList name="function">
              <ParameterList name="function-linear">
		<Parameter name="y0" type="double" value="0."/>
		<Parameter name="gradient" type="Array(double)" value="{0., 1., 0.}"/>
              </ParameterList>
            </ParameterList>
          </ParameterList>
	</ParameterList>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>

      <ParameterList name="field2">
        <!-- values(0) = (2,4) -->
        <!-- values(1) = (2,4) -->
        <!-- values(2) = (3,5) -->
        <!-- values(3) = (4,6) -->
        <!-- values(4) = (4,6) -->
	<Parameter name="evaluator type" type="string" value="independent variable from file"/>
        <Parameter name="filename" type="string" value="test/state_evaluators_functions.h5"/>
        <Parameter name="variable name" type="string" value="field2"/>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>


      <ParameterList name="field3">
        <!-- values(0) = (2,4) -->
        <!-- values(1) = (3,5) -->
        <!-- values(2) = (4,6) -->
        <!-- values(3) = (4,6) -->
        <!-- values(4) = (4,6) -->
	<Parameter name="evaluator type" type="string" value="independent variable from file"/>
        <Parameter name="filename" type="string" value="test/state_evaluators_functions.h5"/>
        <Parameter name="variable name" type="string" value="field2"/>
        <ParameterList name="time function">
          <ParameterList name="function-linear">
	    <Parameter name="y0" type="double" value="0."/>
	    <Parameter name="gradient" type="Array(double)" value="{2.}"/>
          </ParameterList>
        </ParameterList>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>

      <ParameterList name="field4">
        <!-- values(0) = (3,7) -->
        <!-- values(1) = (3,7) -->
        <!-- values(2) = (4,8) -->
        <!-- values(3) = (5,9) -->
        <!-- values(4) = (5,9) -->
	<Parameter name="evaluator type" type="string" value="additive"/>
	<Parameter name="dependencies" type="Array(string)" value="{field1,field2}"/>
	<Parameter name="dependency tags are my tag" type="bool" value="true"/>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>

      <ParameterList name="field4">
        <!-- values(0) = (4,10) -->
        <!-- values(1) = (4,10) -->
        <!-- values(2) = (5,11) -->
        <!-- values(3) = (6,12) -->
        <!-- values(4) = (6,12) -->
	<Parameter name="evaluator type" type="string" value="additive"/>
	<Parameter name="dependencies" type="Array(string)" value="{field1,field2}"/>
	<Parameter name="dependency tags are my tag" type="bool" value="true"/>
        <Parameter name="field1 coefficient" type="double" value="2"/>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>

      <ParameterList name="field5">
        <!-- values(0) = (2,12) -->
        <!-- values(1) = (2,12) -->
        <!-- values(2) = (3,15) -->
        <!-- values(3) = (4,18) -->
        <!-- values(4) = (4,18) -->
	<Parameter name="evaluator type" type="string" value="multiplicative"/>
	<Parameter name="dependencies" type="Array(string)" value="{field1,field2}"/>
	<Parameter name="dependency tags are my tag" type="bool" value="true"/>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>

      <ParameterList name="field6">
        <!-- values(0) = (8,18) -->
        <!-- values(1) = (8,18) -->
        <!-- values(2) = (11,21) -->
        <!-- values(3) = (14,24) -->
        <!-- values(4) = (14,24) -->
	<Parameter name="evaluator type" type="string" value="secondary variable from function"/>
	<Parameter name="dependencies" type="Array(string)" value="{field1,field2}"/>
	<Parameter name="dependency tags are my tag" type="bool" value="true"/>

        <ParameterList name="functions">
          <ParameterList name="field6">
            <ParameterList name="function-linear">
	      <Parameter name="y0" type="double" value="0."/>
	      <Parameter name="gradient" type="Array(double)" value="{2., 3.}"/>
            </ParameterList>
          </ParameterList>
        </ParameterList>
        <ParameterList name="verbose object">
          <Parameter name="verbosity level" type="string" value="extreme" />
        </ParameterList>
      </ParameterList>
      
      
    </ParameterList>
  </ParameterList>
</ParameterList>  
